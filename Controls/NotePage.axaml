<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:NotABook"
             mc:Ignorable="d" d:DesignWidth="960" d:DesignHeight="600"
             Background="{DynamicResource LightBackground950}"
             x:Class="NotABook.NotePage">
<<<<<<< HEAD
	<Grid Margin="10">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="300"/>
		</Grid.ColumnDefinitions>
		
		<Grid Grid.Column="0" Background="{DynamicResource LightBackground950}">
			<local:NoteViewControl />
		</Grid>
		
		<Grid Grid.Column="1" Width="300">
			<local:SidebarControl/>
=======
	<Grid Width="900" Height="600" VerticalAlignment="Center">
		<Grid Name="NoteGrid">
			<Grid Width="590" HorizontalAlignment="Left" Background="{DynamicResource LightBackground950}">
				<TextBox x:Name="NoteText" Background="{DynamicResource LightBackground900}" TextWrapping="Wrap" AcceptsReturn="True" Height="500" Margin="20 50" VerticalAlignment="Top"/>
				<TextBox x:Name="NoteTitle" Background="{DynamicResource LightBackground900}" TextWrapping="Wrap" AcceptsReturn="True" Height="30" Margin="20 10" VerticalAlignment="Top"/>
				<Button x:Name="SaveButton" Background="{DynamicResource LightPrimary600}" Content="Save" Width="100" Height="30" Margin="20 10" VerticalAlignment="Bottom" HorizontalAlignment="Right" HorizontalContentAlignment="Center" Foreground="GhostWhite"/>
				<Button x:Name="DeleteButton" Background="{DynamicResource LightPrimary600}" Content="Delete" Width="100" Height="30" Margin="20 10" VerticalAlignment="Bottom" HorizontalAlignment="Center" HorizontalContentAlignment="Center" Foreground="GhostWhite"/>
				<Button x:Name="ViewButton" Background="{DynamicResource LightPrimary600}" Content="View" Width="100" Height="30" Margin="20 10" VerticalAlignment="Bottom" HorizontalAlignment="Left" HorizontalContentAlignment="Center" Foreground="GhostWhite"/>
				<Popup x:Name="NotePopup" Placement="Center" PlacementTarget="{Binding ElementName=NoteText}" IsOpen="False">
					<Border Background="BurlyWood" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Padding="10">
						<StackPanel>
							<Button Content="X" Click="ClosePopup_Click" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0"/>
							<TextBlock x:Name="NotePopupText" Text="Note Information" HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</StackPanel>
					</Border>
				</Popup>
			</Grid>
			<Grid Width="300" HorizontalAlignment="Right">
				<Grid Height="295" VerticalAlignment="Top">
					<local:CalendarControl />
				</Grid>
				<Grid Height="295" VerticalAlignment="Bottom">
					<local:NoteListControl />
				</Grid>
			</Grid>
>>>>>>> 7fe20f751f5e7c81a8b73fb986129f486eeb6386
		</Grid>
	</Grid>
</UserControl>


<!--- <TextBlock Text="Note Data" HorizontalAlignment="Center" VerticalAlignment="Top"/> -->